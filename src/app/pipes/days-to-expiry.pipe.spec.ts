import { DaysToExpiryPipe } from './days-to-expiry.pipe';

describe('DaysToExpiryPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysToExpiryPipe();
    expect(pipe).toBeTruthy();
  });
});
